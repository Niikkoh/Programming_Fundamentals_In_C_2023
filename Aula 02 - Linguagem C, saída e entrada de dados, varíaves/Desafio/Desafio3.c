#include <stdio.h>

int main ()
{
    int fator;

    printf("Insira um numero inteiro: ");

    scanf("%d",&fator);

    printf("O fator %d ao cubo e: %d",fator, (fator*fator*fator) );

    return 0;
}
